# ゲームエンジン プログラム説明

## はじめに
今回は実装した機能の中から、特に力を入れた5つの機能について説明します。

## 1. アニメーション再生システム

GLTFフォーマットに対応したスキンメッシュアニメーションシステムを作成しました。キャラクターの歩行モーションなどを滑らかに再生できます。

### 工夫した点
```cpp
// アニメーションデータの構造
struct Animation {
    float duration;  // アニメーションの長さ（秒）
    std::map<std::string, NodeAnimation> nodeAnimations;  // ボーン名でアクセス
};
```

最初は全フレームを線形探索していたんですが、めちゃくちゃ重かったので二分探索に変更しました。あと、同じアニメーションを複数のキャラで使い回せるようにして、メモリ使用量を削減しています。

`AnimationPlayer`クラスでは、Play/Pause/Stopといった基本的な制御に加えて、ループ再生にも対応させました。ゲーム開発では必須の機能だと思ったので。

## 2. グレースケールフィルター

画面をモノクロにするポストエフェクトを作成しました。ただ単純に白黒にするんじゃなくて、人間の目の特性を考慮した変換をしています。

### 実装の詳細
```hlsl
// 人間の目は緑に敏感で、青には鈍感
float32_t gray = 0.2126f * output.color.r + 0.7152f * output.color.g + 0.0722f * output.color.b;
```

これはITU-R BT.709という規格に基づいた係数です。単純に(R+G+B)/3とかやると不自然な見た目になっちゃうので....

実装はピクセルシェーダーでやってるので、CPUに負荷をかけずに高速に処理できます。

## 3. Blenderモデルの読み込み

### 対応フォーマット
- **GLB形式**: glTFのバイナリ版。テクスチャも一緒に埋め込めるので便利
- **カスタムJSON**: シーン構成用に独自フォーマットも作りました

### 苦労した点
Blenderの座標系（Zが上）とDirectX（Yが上）で違うので、変換処理を入れる必要がありました。座標計算がすごく苦手でモデルが横向きになったり
さかさまになったりとすごく苦戦をしました。

```cpp
// 同じモデルを何度も読み込まないようにキャッシュ
std::unordered_map<std::string, std::unique_ptr<Model>> modelCache_;
```

読み込みエラーが起きたときに原因がわかりやすいよう、詳細なエラーメッセージを出力するようにしています。

## 4. 環境マップ（Skybox）

### 実装内容
DDSフォーマットのキューブマップを使った環境マッピングです。空の表現だけじゃなく、金属の反射とかにも使えます。

### 便利機能を追加
```cpp
// 新しいオブジェクトに自動で環境マップを適用
template<typename T>
static void SetEnvMap(T* object) {
    if (object && IsEnvironmentTextureLoaded()) {
        object->SetEnvironmentTexture(GetCurrentEnvironmentTexturePath());
    }
}
```

毎回手動で環境マップを設定するのが面倒だったので、自動適用機能を作りました。

## 5. VHSノイズエフェクト（ホラー演出）

ホラーゲームを作りたくて、昔のビデオテープみたいな不気味な映像効果が欲しかったので作りました。

### 実装した効果
1. **垂直バーの歪み**: VHSテープが傷んだときの縦線
2. **色ズレ**: RGBがバラバラにずれる感じ
3. **ノイズ**: 砂嵐とかスキャンライン

### こだわりポイント
```hlsl
// 時間で変化する歪み（複数の周期を組み合わせて自然な動きに）
for (float i = 0.0; i < 0.71; i += 0.1313) {
    float d = fmod(time * i * 0.1, 1.7);
    float o = sin(1.0 - tan(time * 0.024 * i));
    // ...
}
```

複数の三角関数を組み合わせて、予測できない動きを作りました。

血のエフェクトも実装してます。画面の端から赤い染みが広がる感じ。`bloodAmount`パラメータで強度を調整できるので、ゲームの進行に合わせて徐々に恐怖感を演出できます。

### パフォーマンスの工夫
疑似乱数生成は計算が重いので、GPUでも高速に動くアルゴリズムを選びました：
```hlsl
float rand(float32_t2 co) {
    return frac(sin(dot(co.xy, float32_t2(12.9898, 78.233))) * 43758.5453);
}
```


## 最後に

このエンジンは実際にゲームを作ることを想定して開発しました。機能を作るだけじゃなく、使いやすさも考えて設計しています。まだまだ改善点はありますが、商用レベルのゲーム開発にも使えるクオリティを目指しました。

コードはGitHubで公開しているので、詳細はそちらを見ていただければと思います。